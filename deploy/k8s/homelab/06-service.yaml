apiVersion: v1
kind: Service
metadata:
  name: hermes-cloud-agent
  namespace: hermes-olufunke
  labels:
    app.kubernetes.io/name: hermes-cloud-agent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: hermes-cloud-agent
  ports:
    - name: http
      port: 10000
      targetPort: http
